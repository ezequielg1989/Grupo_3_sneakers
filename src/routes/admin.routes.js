// const express = require('express')
// const controller = require('../controllers/productController')
// const router = express.Router()
// const path = require("path");
// const uploadFile = require('../middlewares/multer.js');


// router.get("/admin",controller.homeAdmin)

// router.get("/addProd", controller.admin);//ruta al formulario para sumar un producto
// router.post("/addProd",uploadFile.single("imagenProd"), controller.newProd);//procesa la suma de un nuevo producto

// router.get("/addUser", controller.newUser);//ruta al formulario para sumar un producto
// router.post("/addUser", controller.createUser);//procesa la suma de un nuevo producto

// router.get("/:id", controller.getEditAdmin);//ruta al formulario de edicion de producto
// router.put("/:id", uploadFile.single("imagenProd"),controller.editProd)//ruta q edita el formulario y lo carga
// router.delete("/:id",controller.deleteProd);



// module.exports = router